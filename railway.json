{
  "build": {
    "backend": {
      "installCommand": "pip install -r requirements.txt",
      "buildCommand": "",
      "rootDir": "backend"
    }
  },
   "start": {
      "backend": {
        "startCommand": "web: gunicorn lavergne_backend.wsgi:application --bind 0.0.0.0:$PORT",
        "rootDir": "backend"
      }
    }
}
